# Screenshots

Images referenced from pull requests and issue comments. This branch carries no
code — it exists so a review can *show* a rendered surface instead of describing
one, because `gh` cannot upload to `user-images.githubusercontent.com`: that
endpoint is browser drag-and-drop only and is not exposed to a token.

Reference them as:

    https://raw.githubusercontent.com/HarnessDesk/HarnessDesk/assets/screenshots/<path>
