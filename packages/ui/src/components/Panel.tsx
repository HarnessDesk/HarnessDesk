/**
 * Compatibility entrypoint for the inspector surfaces.
 *
 * The anatomy belongs to the design system; existing feature modules keep
 * this import while the public component owns every visible role.
 */
export {
  Counts,
  DayLabel,
  GroupLine,
  PanelBody,
  PanelEmpty,
  PanelFilter,
  PanelFooter,
  PanelFrame,
  PanelPill,
  PanelRow,
  PanelRowDetail,
  PanelTools,
  RowTime,
  RunDot,
} from '../design'
