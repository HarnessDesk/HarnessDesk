# PR 748: isolated packaged-app recheck

Conversation, editor, terminal, board, room, browser and before-Git product source: fba694c269e8cc721207df5576f43e2ecc12c446. Their capture-driver source is f2c5379796e2cb2e8c4107537b129f14b86545b9.

Repaired Git and About product/capture-driver source: 3596c6c9cc4ab25809e62d63ad6fdc055c2af377. The targeted rerun also propagates the packaged executable into the appearance relaunch. The earlier sweep's About/relaunch used repository Electron and is not published as packaged-app evidence here.

These images use the built, ad-hoc-signed macOS arm64 app, not a browser mock. Main viewport: 1440 by 900 CSS pixels at 2x. About is a separate native window and uses the persisted editorial/violet/round/studio appearance; other frames use the default foundation.

The camera ACP agents have namespaced IDs so installed-vendor discovery cannot replace their scripted processes. Built-in Codex uses the repository's fake app-server and an isolated home. The frame audit checks loaded and hidden history against the exact staged repository roots, as well as visible text and account identities. Each published frame was also visually inspected. They contain invented repositories, scripted conversation text, and only the project's public demo identities. No user-supplied screenshot is uploaded.

This selected set refreshes seven main destinations in both themes: conversation, Git history, editor, terminal, board, room, and browser. Adjacent metrics record computed styles and runtime-ID provenance. About is included separately. Settings and the measured original/broken/repaired comparisons remain in the preceding evidence directory; they are not relabeled as new captures.

The normal first-run import offer is dismissed through its regular store action before capture; error notices are not hidden. The editor must paint the exact synthetic package.json content in CodeMirror in each theme, not merely open a named tab. The fake app-server's in-memory filesystem is seeded through the normal file-save protocol. Terminal uses the plain system shell, and browser loads an invented local page in the production webview.

The full native sweep covered 36 app frames and ran layout/privacy assertions. Visual review identified the touching Git scope labels despite those checks passing. The targeted packaged rerun covers repaired Git in both themes plus About and verifies appearance persistence. Its report is report.json. This directory publishes 17 images, 16 adjacent measurement files, and that report. It does not claim real-vendor authentication or execution, notarized release testing, or a human VoiceOver session.

## Git scope comparison

The before-Git frames came from the full sweep after the conversation scene; the repaired pair is a standalone Git capture with the empty composer behind it. Compare the same right-hand Git pane, not the unrelated transcript state. Both use the same viewport and synthetic repository.

The browser regression fixture measured the broken hand-rolled content-sized buttons at 0px horizontal padding, 15px height and a 2px text gap. Canonical Segmented restores 8px padding on each side, 26px height and a 16px text gap in that preview. Its label is 13px there. The packaged default Desk appearance pictured here uses 12px medium Geist-first labels with 18px line-height, 12px horizontal padding, and the same 26px height; the adjacent native metrics record these values. The selected segment uses the shared surface and a 0 1px 2px / 8% shadow rather than adding elevation to the whole Git pane. Arrow-key focus, Space activation and re-pressing the selected option are covered by regression tests; a re-press does not clear the required Git scope.
